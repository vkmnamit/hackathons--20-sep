[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm --prefix frontend install --include=dev --include=optional"
]

[phases.build]
cmds = [
  "npm --prefix frontend run build"
]

[start]
cmd = "node backend/server.js"
